export default async function globalSetup() {
  // Global setup - can be used to start servers, emulators, etc.
  // Currently tests run against real Firebase
  console.log('Running tests against real Firebase backend');
}
